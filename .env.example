# ===========================================
# StreamVault - Environment Configuration
# ===========================================
# Copy this file to .env and update the values
# cp .env.example .env

# ===========================================
# REQUIRED - Security
# ===========================================
# PocketBase encryption key (MUST be exactly 32 characters)
# Generate with: openssl rand -hex 16
PB_ENCRYPTION_KEY=your-32-character-encryption-key

# ===========================================
# Application Configuration
# ===========================================
# Version tag for Docker images
VERSION=latest

# Application name (displayed in UI)
NEXT_PUBLIC_APP_NAME=StreamVault

# ===========================================
# Network Configuration
# ===========================================
# FQDN for your reverse proxy (Nginx Proxy Manager, Traefik, etc.)
# This is the public URL users will access
# Example: streamvault.mydomain.com
FQDN=streamvault.local

# PocketBase URL (as seen by the browser through your reverse proxy)
# Use your FQDN with the appropriate protocol
# Example: https://streamvault.mydomain.com
NEXT_PUBLIC_POCKETBASE_URL=https://streamvault.local

# ===========================================
# Ports (internal container ports)
# ===========================================
# Backend port - point your reverse proxy here
PB_PORT=8090

# Frontend port - point your reverse proxy here
FRONTEND_PORT=3000

# ===========================================
# Backend Configuration
# ===========================================
# Whisper model for speech recognition (subtitles)
# Options: tiny, base, small, medium, large
# Larger = more accurate but slower and more RAM
WHISPER_MODEL=base

# ===========================================
# External Services (Optional)
# ===========================================
# Ollama URL for AI-powered subtitle translation
# Leave empty to disable translation feature
# Example: http://192.168.1.100:11434 or http://ollama.local:11434
OLLAMA_HOST=

# Ollama model for translation (if OLLAMA_HOST is set)
OLLAMA_MODEL=llama3.2

# ===========================================
# Reverse Proxy Configuration
# ===========================================
# This application expects you to use your own reverse proxy
# (Nginx Proxy Manager, Traefik, Caddy, etc.)
#
# Configure your reverse proxy with:
#   - Frontend: proxy to http://<docker-host>:${FRONTEND_PORT}
#   - API: proxy /api/* and /_/* to http://<docker-host>:${PB_PORT}
#
# Example Nginx Proxy Manager setup:
#   Domain: ${FQDN}
#   Scheme: http
#   Forward Hostname/IP: <docker-host-ip>
#   Forward Port: ${FRONTEND_PORT}
#   Custom locations:
#     /api/* -> http://<docker-host-ip>:${PB_PORT}
#     /_/*   -> http://<docker-host-ip>:${PB_PORT}
